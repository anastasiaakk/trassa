# Сборка установщика: apiUrl задаётся в release-config.json → .env.production.local (apply-release-config).
# Обычный vite build без установщика — локальный API (dev / server:dev).
VITE_USE_AUTH_API=true
