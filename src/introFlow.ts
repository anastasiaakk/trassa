/** Длительность сплэша (entry-splash-bg.mp4 обрезан до 4 с) */
export const ENTRY_SPLASH_DURATION_MS = 4000;
export const ENTRY_SPLASH_MIN_MS = 3600;
/** Максимум показа ролика (реальное время на экране) */
export const ENTRY_SPLASH_PLAY_MS = 4000;
export const ENTRY_SPLASH_MAX_MS = 4200;
/** Скорость воспроизведения entry-splash-bg.mp4 (1 = как в исходнике) */
export const ENTRY_SPLASH_PLAYBACK_RATE = 1;
/** Ждём буфер перед стартом; на медленной сети — пропуск сплэша */
export const ENTRY_SPLASH_LOAD_TIMEOUT_MS = 12000;
/** Кроссфейд сплэш → Page1 */
export const ENTRY_SPLASH_FADE_MS = 750;
